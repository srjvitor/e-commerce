const { Router } = require('express');

//const HomeController = require('./app/controllers/HomeController');

const routes = new Router();

//rotas.get('/home', HomeController.index);

module.exports = routes;